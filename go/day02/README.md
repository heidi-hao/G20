# 复合数据结构

之前讲了基础类型 接下来讲复合数据结构：数组 结构体 切片 Map

很多现实的场景 都是复合数据结构 比如
+ 数组：ip地址 192.168.1.1 就是4个整数的数组
+ 切片：长度不固定的数组，一堆服务器ip['192.168.1.1','192.168.1.1']
+ HashMap(字典,Map):健值对['key':'value','key':'value'],属性不固定的，服务器{'cpu':'1核','':''}
+ 结构体：健值对{}